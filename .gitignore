# Dependencies
node_modules/

# Environment variables (NEVER commit these)
.env
.env.local
.env.production

# Vercel
.vercel

# Local agent credentials (private keys — never commit)
credentials.json

# OS files
.DS_Store

# Logs
*.log
npm-debug.log*
.env*.local
